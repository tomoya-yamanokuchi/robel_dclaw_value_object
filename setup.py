from setuptools import setup, find_packages

setup(
    name='robel_dclaw_value_object',            # パッケージ名
    version='0.1',                # バージョン番号
    packages=find_packages(),     # パッケージを自動的に探してインクルード
)
